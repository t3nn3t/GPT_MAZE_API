

class TestManager:

    def test(maze_dataset, repeats):
        
        solved = 0
        total = len(maze_dataset) * repeats

        



        score = (solved/total) * 100

        return score